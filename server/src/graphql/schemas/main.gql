type Query {
  hello: String!
  posts(newest: Boolean!): [Post]!
}

type Mutation {
  addHello(content: String!): String!
  createPost(title: String!, content: String): String!
}

type Post {
  id: ID!
  createdAt: String!
  updatedAt: String!
  title: String!
  content: String
}
